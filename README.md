# nodelogicau/homebrew-tap

Homebrew casks for NodeLogic tools (macOS).

```sh
brew install nodelogicau/tap/particulars
```

Casks under `Casks/` are published automatically by each project's release
pipeline (GoReleaser); do not edit them by hand.

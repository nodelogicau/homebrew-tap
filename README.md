# nodelogicau/homebrew-tap

Homebrew formulae for NodeLogic tools.

```sh
brew install nodelogicau/tap/particulars
```

Formulae under `Formula/` are published automatically by each project's release
pipeline (GoReleaser); do not edit them by hand.
